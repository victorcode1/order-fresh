from setuptools import setup, find_packages

setup(
    name='pyKDSAPI',
    version='0.1.0',
    description='Fresh KDS API client',
    author='Tu Nombre',
    packages=find_packages(),
    install_requires=['requests'],
)
